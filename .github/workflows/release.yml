name: release

# Managed by maelys-release untagged (0.37.0). Regenerate with
# 'maelys-release adopt' of maelys-release; do not edit by hand.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing signed tag whose release and Homebrew publication are replayed
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    uses: maelys-dev/maelys-release/.github/workflows/release.yml@6125242502e5216bbea7a0dd596adc0575ac3f0a # untagged
    permissions:
      contents: write
      id-token: write
      attestations: write
    with:
      product: maelys-datalog
      tag: ${{ inputs.tag || github.ref_name }}
      verify_command: bash scripts/verify-release.sh TARGET
      linux_packages: build-essential dpkg-dev file rpm clang jq
      targets: '[{"target": "linux-x86_64"}, {"target": "linux-arm64"}, {"target": "macos-arm64"}, {"target": "wasm32", "runner": "ubuntu-26.04"}]'
      manifest_patterns: '*.tar.gz *.deb *.rpm release-receipt-*.json'

  channel-npm:
    needs: release
    if: needs.release.result == 'success'
    uses: maelys-dev/maelys-release/.github/workflows/channel.yml@6125242502e5216bbea7a0dd596adc0575ac3f0a # untagged
    permissions:
      contents: write
      packages: write
    with:
      product: maelys-datalog
      tag: ${{ inputs.tag || github.ref_name }}
      channel: npm
      publish_command: bash scripts/publish-channel.sh TAG CHANNEL
