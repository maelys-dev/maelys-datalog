# maelys-datalog

Standalone extraction staging directory for the Maelys bounded Datalog engine.

This directory is intentionally ordinary source during
`P3-C28-DATALOG-STANDALONE-EXTRACT`. Conversion to a git submodule is deferred
until the acceptance OV.
