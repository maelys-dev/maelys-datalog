% MAELYS-DATALOG-v2 representative source
safe("alice").
allow(X) :- user(X), not(blocked(X)).
path(X, Y) :- edge(X, Y).
has_edge(X) :- edge(X, _).
path(X, Y) :- edge(X, Y) or owns(X, Y).
score_ok(X) :- score(X, S), S * 2 + 1 >= 7.
